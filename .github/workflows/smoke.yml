name: Smoke

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: smoke-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

defaults:
  run:
    shell: bash

jobs:
  smoke:
    name: Startup smoke
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      NEXT_PUBLIC_API_BASE_URL: http://127.0.0.1:8000/api/v1
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Set up uv
        uses: astral-sh/setup-uv@v5

      - name: Install workspace dependencies
        run: pnpm install --frozen-lockfile

      - name: Sync API dependencies
        run: uv sync --project services/api --extra dev

      - name: Build web app
        run: pnpm build:web

      - name: Start API server
        run: |
          smoke_tmp_dir="$GITHUB_WORKSPACE/.smoke-tmp"
          codex_home="$smoke_tmp_dir/codex-home"
          data_dir="$smoke_tmp_dir/data"

          mkdir -p "$codex_home" "$data_dir"
          CODEX_REPLAY_CODEX_HOME="$codex_home" \
          CODEX_REPLAY_DATA_DIR="$data_dir" \
          uv run --project services/api \
            uvicorn app.main:app \
            --app-dir services/api \
            --host 127.0.0.1 \
            --port 8000 \
            >"$smoke_tmp_dir/api.log" 2>&1 &
          echo $! > "$smoke_tmp_dir/api.pid"

      - name: Start web server
        run: |
          smoke_tmp_dir="$GITHUB_WORKSPACE/.smoke-tmp"
          pnpm --filter web exec next start --hostname 127.0.0.1 --port 3000 \
            >"$smoke_tmp_dir/web.log" 2>&1 &
          echo $! > "$smoke_tmp_dir/web.pid"

      - name: Run smoke checks
        run: bash scripts/ci/run-smoke.sh

      - name: Upload smoke logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-logs
          path: |
            .smoke-tmp/api.log
            .smoke-tmp/web.log

      - name: Stop background services
        if: always()
        run: |
          smoke_tmp_dir="$GITHUB_WORKSPACE/.smoke-tmp"
          if [[ -f "$smoke_tmp_dir/web.pid" ]]; then
            kill "$(cat "$smoke_tmp_dir/web.pid")" || true
          fi
          if [[ -f "$smoke_tmp_dir/api.pid" ]]; then
            kill "$(cat "$smoke_tmp_dir/api.pid")" || true
          fi
